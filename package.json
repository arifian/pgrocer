{
  "name": "pgrocer",
  "version": "0.1.0",
  "description": "Project Gorgon grocery list generator",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "scrape": "tsx src/cli/scrape.ts",
    "grocer": "tsx src/cli/grocer.ts",
    "build": "tsc",
    "web:dev": "npm -w web run dev",
    "web:build": "npm -w web run build",
    "web:deploy": "npm -w web run deploy",
    "cache:sync": "node scripts/sync-cache.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
